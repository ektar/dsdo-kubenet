---
apiVersion: v1
kind: Service
metadata:
  labels:
    app: terminal
  name: terminal
  namespace: dsdo-system
spec:
  ports:
    - port: 22
      protocol: TCP
      name: ssh
  selector:
    app: terminal
    state: blue